<h1>管理画面のユーザーの一覧</h1>

<table>
<tr>
  <th>ID</th>
  <th>ユーザー名</th>
  <th>登録したタスクの数</th>
</tr>


<% @users.each do |user| %>
  <tr>
    <th><%= user.id %></th> 
    <td><%= user.name %></td>
    <td><%= user.tasks.size %></td>
  </tr>
<% end %>
</table>

